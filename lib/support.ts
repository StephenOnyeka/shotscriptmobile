/**
 * Placeholder inbox — swap for the real one before launch. Kept in its own
 * module so both the server page and the client form read the same value.
 */
export const SUPPORT_EMAIL = "support@shotscript.app";
